[package]
name = "avelune"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
publish = false
description = "Canonical Rust implementation of Avelune Draft Generation codecs and container"
readme = "../../README.md"

[dependencies]
avelune-kernels = { path = "../avelune-kernels" }

[target.'cfg(not(target_arch = "wasm32"))'.dependencies]
rayon = "1.12.0"

[dev-dependencies]
avelune-reference = { path = "../avelune-reference" }
proptest = "1.11.0"

[lints]
workspace = true
