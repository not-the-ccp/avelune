name: Codec benchmark history
on:
  workflow_dispatch:
  schedule:
    - cron: '13 2 * * 0'
permissions:
  contents: read
jobs:
  benchmark:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Install Rust 1.97.1
        run: rustup toolchain install 1.97.1 --profile minimal && rustup default 1.97.1
      - name: Install FFmpeg
        run: sudo apt-get update && sudo apt-get install -y ffmpeg
      - name: Build canonical CLI
        run: cargo build -p avelune-cli --release --locked
      - name: Deterministic multi-rate history artifacts
        run: |
          mkdir -p results/history
          for q in 1 48 96 192; do
            python3 scripts/ci-media-regression.py --cli target/release/avelune --q "$q" --repeats 5 --out-json "results/history/canonical-q${q}.json" --out-csv "results/history/canonical-q${q}.csv"
          done
      - name: Implementation lab
        run: cargo run -p avelune-lab --release --locked -- --scale 5 --json results/history/canonical-lab.json --csv results/history/canonical-lab.csv
      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: codec-benchmark-${{ github.sha }}
          retention-days: 90
          path: results/history/
